meta:
  generated_by: stop-guessing caiq derive
  note: 'DERIVED from proofs. Do not hand-edit: every answer is computed from the claims that are proven
    in the ledger, and every evidence ref is a ledger record id that `stop-guessing claims check` re-verifies.
    Editing this file by hand reverses the direction of causation the toolchain exists to enforce.'
  claims_proven: 20/20
  claims_scope: APPLICATION claims only. The release-attestation claim (CLAIM-21) derives and fills this
    document, so it cannot appear in a count this document states — its proof record does not exist until
    after this file is written. It is reported by `stop-guessing attest --self`, not here.
  release_attestation:
  - CLAIM-21
  published_controls_answered: 11
  proposed_controls_recorded: 3
  chain_intact: true
  chain_keyed: true
  aicm_version: 1.1.0
  caiq_version: 1.1.0
answers:
- control: A&A-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: '`verify --sufficiency` reports `incomplete` and names the unpopulated evidence regime
    rather than claiming sufficiency it cannot back; The carried AI-CAIQ version is inspected on every
    run by parsing cell A1''s JSON blob, not by trusting the sheet name, and a drifted workbook refuses
    regeneration. SURFACE RESTORED 2026-08-05: hook:SessionStart was withdrawn from this claim while closing
    an audit finding, which narrowed the claim to fit the metric instead of reporting that the proof does
    not drive the deployed hook. It is declared again and is NOT exercised, so this claim is UNPROVEN
    until a proof drives the hook in a real process; Every distributed surface — plugin, skill, slash-command
    and CLI — is accounted for by a live-run proof recorded in this toolchain''s own ledger. NARROWED
    2026-08-04 (SG-HARD-045). "Accounted for" is deliberately weaker than "exercised": CLI surfaces are
    invoked, but the proof accepts exit code 1 as a successful exercise, runs repository modules rather
    than a clean marketplace install, and establishes plugin, skill and command surfaces by manifest and
    file presence rather than by loading or invoking them. Clean-environment black-box matrices are required
    before this claims per-surface behaviour. SURFACE RESTORED 2026-08-05: plugin:stop-guessing, skill:stop-guessing,
    command:/custody was removed from this claim while closing an audit finding, which narrowed the assertion
    to fit the metric rather than reporting the gap. IMPLEMENTATION_PLAN.md §2.1 names plugins, skills
    and slash-commands as things that must be PROVEN TO RUN, and §15 names `demo` as the single command
    a reviewer runs. The surface is declared again and is NOT exercised: `surface_validated` is false
    because it is false. Proven by 3 record(s) in this toolchain''s own keyed chain-of-custody ledger
    (sg:1377:f2e620b51f56ba80, sg:1393:756cb0fadce91a5c, sg:1405:ac40f4ea3e73ae01), each produced by an
    executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-06
  - CLAIM-14
  - CLAIM-20
  evidence:
  - type: ledger
    ref: sg:1377:f2e620b51f56ba80
  - type: ledger
    ref: sg:1393:756cb0fadce91a5c
  - type: ledger
    ref: sg:1405:ac40f4ea3e73ae01
- control: CCC-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'The delegation sequence enforces test-before-live-data, and refuses a script whose
    digest changed after its test passed. NARROWED 2026-08-04 (SG-HARD-018/019). This holds against a
    cooperative script, not an adversarial one. The security subject is a mutable pathname, so a concurrent
    replacer can swap the bytes between the digest check and execution; and the paired test itself runs
    unsigned and can rewrite the script it has just certified. Immutable content-addressed execution is
    required before this is a security property rather than a discipline; Superseding no-noodles is byte-compatible:
    every payload in the corpus produces an identical (exit_code, stdout) through the dispatcher as through
    the standalone hooks; Every no-noodles surface keeps working after supersession — the three escape
    markers, the config chain, `grant_session_trust.sh`, `/no-noodle`, `/noodle-options`, and `risk_summary.py`
    parsing `observations.jsonl`. SURFACE RESTORED 2026-08-05: command:/no-noodle, command:/noodle-options
    was removed from this claim while closing an audit finding, which narrowed the assertion to fit the
    metric rather than reporting the gap. IMPLEMENTATION_PLAN.md §2.1 names plugins, skills and slash-commands
    as things that must be PROVEN TO RUN, and §15 names `demo` as the single command a reviewer runs.
    The surface is declared again and is NOT exercised: `surface_validated` is false because it is false.
    Proven by 3 record(s) in this toolchain''s own keyed chain-of-custody ledger (sg:1383:56a30014dd4fb1ef,
    sg:1397:6be771a1609f1f04, sg:1399:2bed589e4c313ad9), each produced by an executable procedure and
    re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-09
  - CLAIM-16
  - CLAIM-17
  evidence:
  - type: ledger
    ref: sg:1383:56a30014dd4fb1ef
  - type: ledger
    ref: sg:1397:6be771a1609f1f04
  - type: ledger
    ref: sg:1399:2bed589e4c313ad9
- control: DSP-05
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'The ledger records data flow, not only actions: a read, a transform and a write are
    linked by an explicit derivation edge naming the artifacts and the script that joined them. SURFACE
    RESTORED 2026-08-05: hook:PostToolUse was withdrawn from this claim while closing an audit finding,
    which narrowed the claim to fit the metric instead of reporting that the proof does not drive the
    deployed hook. It is declared again and is NOT exercised, so this claim is UNPROVEN until a proof
    drives the hook in a real process; No statically detectable network call site exists in the shipped
    Python package except the named opt-in anchoring exception. This is a source audit, not a proof of
    no network activity. It cannot see indirect subprocess calls, shell network programs, dynamic imports
    or commands assembled at runtime. SUB-CLAIM WITHDRAWN 2026-08-04 (SG-HARD-039): this also asserted
    "and CI performs no fetch", which is false. CI runs actions/checkout, actions/setup-python and pip
    install, every one of which resolves a remote action or package. The proof equated the absence of
    literal curl and wget text with the absence of fetching. Build-time dependency retrieval is real and
    is not claimed away. SURFACE CORRECTED 2026-08-05 (R2-003/R2-039): this claim declared cli:"stop-guessing
    doctor --network", which the CLI does not provide — argparse rejects it. Replaced with cli:"stop-guessing
    doctor". A claim naming a command that does not exist is an overclaim, and it survived because non-hook
    surfaces were never executed. Proven by 2 record(s) in this toolchain''s own keyed chain-of-custody
    ledger (sg:1367:a97c45f0b06c9f8a, sg:1401:1c4947958496cf25), each produced by an executable procedure
    and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-01
  - CLAIM-18
  evidence:
  - type: ledger
    ref: sg:1367:a97c45f0b06c9f8a
  - type: ledger
    ref: sg:1401:1c4947958496cf25
- control: DSP-20
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'The ledger records data flow, not only actions: a read, a transform and a write are
    linked by an explicit derivation edge naming the artifacts and the script that joined them. SURFACE
    RESTORED 2026-08-05: hook:PostToolUse was withdrawn from this claim while closing an audit finding,
    which narrowed the claim to fit the metric instead of reporting that the proof does not drive the
    deployed hook. It is declared again and is NOT exercised, so this claim is UNPROVEN until a proof
    drives the hook in a real process; Accumulated session taint denies an egress that the identical call
    would have been allowed to make earlier in the same session, the denial names the contributing artifacts,
    and this holds through the real hook across separate processes and with the state cache deleted. SURFACE
    RESTORED 2026-08-05: cli:"stop-guessing demo --posture steer" was removed from this claim while closing
    an audit finding, which narrowed the assertion to fit the metric rather than reporting the gap. IMPLEMENTATION_PLAN.md
    §2.1 names plugins, skills and slash-commands as things that must be PROVEN TO RUN, and §15 names
    `demo` as the single command a reviewer runs. The surface is declared again and is NOT exercised:
    `surface_validated` is false because it is false; `steer` asks on first touch of a classified artifact
    and offers a recorded script delegation; it does not deny the first read. SURFACE RESTORED 2026-08-05:
    hook:PreToolUse was withdrawn from this claim while closing an audit finding, which narrowed the claim
    to fit the metric instead of reporting that the proof does not drive the deployed hook. It is declared
    again and is NOT exercised, so this claim is UNPROVEN until a proof drives the hook in a real process;
    The delegation sequence enforces test-before-live-data, and refuses a script whose digest changed
    after its test passed. NARROWED 2026-08-04 (SG-HARD-018/019). This holds against a cooperative script,
    not an adversarial one. The security subject is a mutable pathname, so a concurrent replacer can swap
    the bytes between the digest check and execution; and the paired test itself runs unsigned and can
    rewrite the script it has just certified. Immutable content-addressed execution is required before
    this is a security property rather than a discipline; Under `bar` the model receives only a handle
    or summary, and an unsigned script is refused. SURFACE RESTORED 2026-08-05: hook:PreToolUse was withdrawn
    from this claim while closing an audit finding, which narrowed the claim to fit the metric instead
    of reporting that the proof does not drive the deployed hook. It is declared again and is NOT exercised,
    so this claim is UNPROVEN until a proof drives the hook in a real process; The blank AI-CAIQ template
    is never modified. A fill leaves its digest and mtime unchanged, and an altered template refuses regeneration.
    Proven by 6 record(s) in this toolchain''s own keyed chain-of-custody ledger (sg:1367:a97c45f0b06c9f8a,
    sg:1379:d802935a895e96f5, sg:1381:505fdfbedbb5e088, sg:1383:56a30014dd4fb1ef, sg:1385:71b5c8293bcf13bc,
    sg:1395:5769d2ad015984b2), each produced by an executable procedure and re-verified by `stop-guessing
    claims check`.'
  claims:
  - CLAIM-01
  - CLAIM-07
  - CLAIM-08
  - CLAIM-09
  - CLAIM-10
  - CLAIM-15
  evidence:
  - type: ledger
    ref: sg:1367:a97c45f0b06c9f8a
  - type: ledger
    ref: sg:1379:d802935a895e96f5
  - type: ledger
    ref: sg:1381:505fdfbedbb5e088
  - type: ledger
    ref: sg:1383:56a30014dd4fb1ef
  - type: ledger
    ref: sg:1385:71b5c8293bcf13bc
  - type: ledger
    ref: sg:1395:5769d2ad015984b2
- control: I&S-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'No statically detectable network call site exists in the shipped Python package except
    the named opt-in anchoring exception. This is a source audit, not a proof of no network activity.
    It cannot see indirect subprocess calls, shell network programs, dynamic imports or commands assembled
    at runtime. SUB-CLAIM WITHDRAWN 2026-08-04 (SG-HARD-039): this also asserted "and CI performs no fetch",
    which is false. CI runs actions/checkout, actions/setup-python and pip install, every one of which
    resolves a remote action or package. The proof equated the absence of literal curl and wget text with
    the absence of fetching. Build-time dependency retrieval is real and is not claimed away. SURFACE
    CORRECTED 2026-08-05 (R2-003/R2-039): this claim declared cli:"stop-guessing doctor --network", which
    the CLI does not provide — argparse rejects it. Replaced with cli:"stop-guessing doctor". A claim
    naming a command that does not exist is an overclaim, and it survived because non-hook surfaces were
    never executed. Proven by 1 record(s) in this toolchain''s own keyed chain-of-custody ledger (sg:1401:1c4947958496cf25),
    each produced by an executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-18
  evidence:
  - type: ledger
    ref: sg:1401:1c4947958496cf25
- control: LOG-03
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'A keyed chain defeats truncate-and-recompute. Without the session key held in the OS
    keychain, a forged chain that is internally consistent still fails verification; A record missing
    the `alterations` key is refused at write, while `alterations: []` is accepted — an empty list asserts
    nothing was altered, a missing key means nobody looked. SURFACE RESTORED 2026-08-05: cli:"stop-guessing
    record emit" was removed from this claim while closing an audit finding, which narrowed the assertion
    to fit the metric rather than reporting the gap. IMPLEMENTATION_PLAN.md §2.1 names plugins, skills
    and slash-commands as things that must be PROVEN TO RUN, and §15 names `demo` as the single command
    a reviewer runs. The surface is declared again and is NOT exercised: `surface_validated` is false
    because it is false; `verify --sufficiency` reports `incomplete` and names the unpopulated evidence
    regime rather than claiming sufficiency it cannot back; Execution claims made by the agent are reconciled
    against the hook-side dispatch ledger; a fabricated or replayed claim is detected and escalated to
    a human. SURFACE RESTORED 2026-08-05: hook:Stop was withdrawn from this claim while closing an audit
    finding, which narrowed the claim to fit the metric instead of reporting that the proof does not drive
    the deployed hook. It is declared again and is NOT exercised, so this claim is UNPROVEN until a proof
    drives the hook in a real process. Proven by 4 record(s) in this toolchain''s own keyed chain-of-custody
    ledger (sg:1369:fa724260dfc6fb61, sg:1375:dc5b338c7cf61d3e, sg:1377:f2e620b51f56ba80, sg:1391:9f29530c0b6bea25),
    each produced by an executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-02
  - CLAIM-05
  - CLAIM-06
  - CLAIM-13
  evidence:
  - type: ledger
    ref: sg:1369:fa724260dfc6fb61
  - type: ledger
    ref: sg:1375:dc5b338c7cf61d3e
  - type: ledger
    ref: sg:1377:f2e620b51f56ba80
  - type: ledger
    ref: sg:1391:9f29530c0b6bea25
- control: LOG-10
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: A keyed chain defeats truncate-and-recompute. Without the session key held in the OS
    keychain, a forged chain that is internally consistent still fails verification; The sink refuses
    to append onto a broken or truncated chain, rather than burying the break under new entries; Sealing
    archives a segment and chains the next segment to the sealed digest; no ledger rotation ever truncates
    history; PATH-shadowing the CLI, direct ledger writes, hook-script substitution and daemon kill are
    each refused or detected. NARROWED 2026-08-04 (SG-HARD-006/007/010). This is no longer the broader
    claim that the recorder is unreachable by the agent it records. At the tiers the installer can actually
    deliver (0 and 1) the recorder runs under the agent's own uid, the chain key is a mode-600 file that
    anything the agent runs can read, and the socket authenticates ordering rather than the caller. The
    four named attacks are refused or detected. An agent that simply reads the key is not among them;
    The blank AI-CAIQ template is never modified. A fill leaves its digest and mtime unchanged, and an
    altered template refuses regeneration; A full uninstall removes hooks and registrations while preserving
    the accumulated ledger and observation log. Proven by 6 record(s) in this toolchain's own keyed chain-of-custody
    ledger (sg:1369:fa724260dfc6fb61, sg:1371:8a2c9814c7c48cdd, sg:1373:056c5788b481d80e, sg:1389:1a1f9ee0a2a009e5,
    sg:1395:5769d2ad015984b2, sg:1403:5103275a02d42445), each produced by an executable procedure and
    re-verified by `stop-guessing claims check`.
  claims:
  - CLAIM-02
  - CLAIM-03
  - CLAIM-04
  - CLAIM-12
  - CLAIM-15
  - CLAIM-19
  evidence:
  - type: ledger
    ref: sg:1369:fa724260dfc6fb61
  - type: ledger
    ref: sg:1371:8a2c9814c7c48cdd
  - type: ledger
    ref: sg:1373:056c5788b481d80e
  - type: ledger
    ref: sg:1389:1a1f9ee0a2a009e5
  - type: ledger
    ref: sg:1395:5769d2ad015984b2
  - type: ledger
    ref: sg:1403:5103275a02d42445
- control: LOG-12
  answer: 'No'
  ssrm: Owned by OSP
  implementation: 'No. There is artifact-level and decision-level provenance, but no continuous activity
    log for cryptographic key lifecycle events specifically. Evidence searched: stop_guessing/attest/keys.py,
    stop_guessing/ledger/, rules/, policy/coc.policy.d/.'
  claims:
  - CLAIM-04
  evidence:
  - type: ledger
    ref: sg:1373:056c5788b481d80e
- control: MDS-13
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'Under `bar` the model receives only a handle or summary, and an unsigned script is
    refused. SURFACE RESTORED 2026-08-05: hook:PreToolUse was withdrawn from this claim while closing
    an audit finding, which narrowed the claim to fit the metric instead of reporting that the proof does
    not drive the deployed hook. It is declared again and is NOT exercised, so this claim is UNPROVEN
    until a proof drives the hook in a real process. Proven by 1 record(s) in this toolchain''s own keyed
    chain-of-custody ledger (sg:1385:71b5c8293bcf13bc), each produced by an executable procedure and re-verified
    by `stop-guessing claims check`.'
  claims:
  - CLAIM-10
  evidence:
  - type: ledger
    ref: sg:1385:71b5c8293bcf13bc
- control: SEF-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'Execution claims made by the agent are reconciled against the hook-side dispatch ledger;
    a fabricated or replayed claim is detected and escalated to a human. SURFACE RESTORED 2026-08-05:
    hook:Stop was withdrawn from this claim while closing an audit finding, which narrowed the claim to
    fit the metric instead of reporting that the proof does not drive the deployed hook. It is declared
    again and is NOT exercised, so this claim is UNPROVEN until a proof drives the hook in a real process.
    Proven by 1 record(s) in this toolchain''s own keyed chain-of-custody ledger (sg:1391:9f29530c0b6bea25),
    each produced by an executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-13
  evidence:
  - type: ledger
    ref: sg:1391:9f29530c0b6bea25
- control: STA-09
  answer: 'No'
  ssrm: Owned by OSP
  implementation: 'No. A service Bill of Materials is not emitted. The in-toto Statement envelope and
    the pinned MANIFEST.sha256 over the vendored tree are adjacent but are not an SBOM. Evidence searched:
    stop_guessing/attest/, stop_guessing/compat/, .github/workflows/.'
  claims:
  - CLAIM-14
  - CLAIM-20
  evidence:
  - type: ledger
    ref: sg:1393:756cb0fadce91a5c
  - type: ledger
    ref: sg:1405:ac40f4ea3e73ae01
proposed_agentic_controls:
  note: 'CSA''s DRAFT agentic controls (labs.cloudsecurityalliance.org). Proposed, not published: they
    do not exist in the AICM v1.1.0 workbook and are NOT written into it. Recorded here because the evidence
    is real and the gap analysis is CSA''s own, but presenting them as AICM controls would be a fabrication.'
  answers:
  - control: IAM-AG-03
    answer: 'Yes'
    ssrm: Owned by OSP
    implementation: PATH-shadowing the CLI, direct ledger writes, hook-script substitution and daemon
      kill are each refused or detected. NARROWED 2026-08-04 (SG-HARD-006/007/010). This is no longer
      the broader claim that the recorder is unreachable by the agent it records. At the tiers the installer
      can actually deliver (0 and 1) the recorder runs under the agent's own uid, the chain key is a mode-600
      file that anything the agent runs can read, and the socket authenticates ordering rather than the
      caller. The four named attacks are refused or detected. An agent that simply reads the key is not
      among them. Proven by 1 record(s) in this toolchain's own keyed chain-of-custody ledger (sg:1389:1a1f9ee0a2a009e5),
      each produced by an executable procedure and re-verified by `stop-guessing claims check`.
    claims:
    - CLAIM-12
    evidence:
    - type: ledger
      ref: sg:1389:1a1f9ee0a2a009e5
  - control: LOG-AG-01
    answer: 'Yes'
    ssrm: Owned by OSP
    implementation: 'The ledger records data flow, not only actions: a read, a transform and a write are
      linked by an explicit derivation edge naming the artifacts and the script that joined them. SURFACE
      RESTORED 2026-08-05: hook:PostToolUse was withdrawn from this claim while closing an audit finding,
      which narrowed the claim to fit the metric instead of reporting that the proof does not drive the
      deployed hook. It is declared again and is NOT exercised, so this claim is UNPROVEN until a proof
      drives the hook in a real process; Session custody state rebuilt from the ledger alone reproduces
      the pre-compaction digest exactly; state never depends on the transcript. SURFACE RESTORED 2026-08-05:
      hook:PreCompact was withdrawn from this claim while closing an audit finding, which narrowed the
      claim to fit the metric instead of reporting that the proof does not drive the deployed hook. It
      is declared again and is NOT exercised, so this claim is UNPROVEN until a proof drives the hook
      in a real process. Proven by 2 record(s) in this toolchain''s own keyed chain-of-custody ledger
      (sg:1367:a97c45f0b06c9f8a, sg:1387:3a4121a96afb3261), each produced by an executable procedure and
      re-verified by `stop-guessing claims check`.'
    claims:
    - CLAIM-01
    - CLAIM-11
    evidence:
    - type: ledger
      ref: sg:1367:a97c45f0b06c9f8a
    - type: ledger
      ref: sg:1387:3a4121a96afb3261
  - control: LOG-AG-02
    answer: 'Yes'
    ssrm: Owned by OSP
    implementation: 'Accumulated session taint denies an egress that the identical call would have been
      allowed to make earlier in the same session, the denial names the contributing artifacts, and this
      holds through the real hook across separate processes and with the state cache deleted. SURFACE
      RESTORED 2026-08-05: cli:"stop-guessing demo --posture steer" was removed from this claim while
      closing an audit finding, which narrowed the assertion to fit the metric rather than reporting the
      gap. IMPLEMENTATION_PLAN.md §2.1 names plugins, skills and slash-commands as things that must be
      PROVEN TO RUN, and §15 names `demo` as the single command a reviewer runs. The surface is declared
      again and is NOT exercised: `surface_validated` is false because it is false. Proven by 1 record(s)
      in this toolchain''s own keyed chain-of-custody ledger (sg:1379:d802935a895e96f5), each produced
      by an executable procedure and re-verified by `stop-guessing claims check`.'
    claims:
    - CLAIM-07
    evidence:
    - type: ledger
      ref: sg:1379:d802935a895e96f5
