meta:
  generated_by: stop-guessing caiq derive
  note: 'DERIVED from proofs. Do not hand-edit: every answer is computed from the claims that are proven
    in the ledger, and every evidence ref is a ledger record id that `stop-guessing claims check` re-verifies.
    Editing this file by hand reverses the direction of causation the toolchain exists to enforce.'
  claims_proven: 20/20
  claims_scope: APPLICATION claims only. The release-attestation claim (CLAIM-21) derives and fills this
    document, so it cannot appear in a count this document states — its proof record does not exist until
    after this file is written. It is reported by `stop-guessing attest --self`, not here.
  release_attestation:
  - CLAIM-21
  published_controls_answered: 11
  proposed_controls_recorded: 3
  chain_intact: true
  chain_keyed: true
  aicm_version: 1.1.0
  caiq_version: 1.1.0
answers:
- control: A&A-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: '`verify --sufficiency` reports `incomplete` and names the unpopulated evidence regime
    rather than claiming sufficiency it cannot back; The carried AI-CAIQ version is inspected on every
    run by parsing cell A1''s JSON blob, not by trusting the sheet name, and a drifted workbook refuses
    regeneration. SURFACE NARROWED 2026-08-05 (R2-003). This claim previously declared hook:SessionStart;
    its proof exercises the mechanism directly rather than driving that hook, and registration is not
    execution. The surface is withdrawn rather than asserted. Deployed-hook behaviour is a separate claim,
    and it has not been made; Every distributed surface — plugin, skill, slash-command and CLI — is accounted
    for by a live-run proof recorded in this toolchain''s own ledger. NARROWED 2026-08-04 (SG-HARD-045).
    "Accounted for" is deliberately weaker than "exercised": CLI surfaces are invoked, but the proof accepts
    exit code 1 as a successful exercise, runs repository modules rather than a clean marketplace install,
    and establishes plugin, skill and command surfaces by manifest and file presence rather than by loading
    or invoking them. Clean-environment black-box matrices are required before this claims per-surface
    behaviour. Proven by 3 record(s) in this toolchain''s own keyed chain-of-custody ledger (sg:851:22f751f9dc2fae59,
    sg:859:02e44cb93e7a4560, sg:865:17fce297e11b3fd7), each produced by an executable procedure and re-verified
    by `stop-guessing claims check`.'
  claims:
  - CLAIM-06
  - CLAIM-14
  - CLAIM-20
  evidence:
  - type: ledger
    ref: sg:851:22f751f9dc2fae59
  - type: ledger
    ref: sg:859:02e44cb93e7a4560
  - type: ledger
    ref: sg:865:17fce297e11b3fd7
- control: CCC-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'The delegation sequence enforces test-before-live-data, and refuses a script whose
    digest changed after its test passed. NARROWED 2026-08-04 (SG-HARD-018/019). This holds against a
    cooperative script, not an adversarial one. The security subject is a mutable pathname, so a concurrent
    replacer can swap the bytes between the digest check and execution; and the paired test itself runs
    unsigned and can rewrite the script it has just certified. Immutable content-addressed execution is
    required before this is a security property rather than a discipline; Superseding no-noodles is byte-compatible:
    every payload in the corpus produces an identical (exit_code, stdout) through the dispatcher as through
    the standalone hooks; Every no-noodles surface keeps working after supersession — the three escape
    markers, the config chain, `grant_session_trust.sh`, `/no-noodle`, `/noodle-options`, and `risk_summary.py`
    parsing `observations.jsonl`. Proven by 3 record(s) in this toolchain''s own keyed chain-of-custody
    ledger (sg:854:8892fdc4cc4bee3f, sg:861:7004f70221a62847, sg:862:1c8867ccb0cdaa2a), each produced
    by an executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-09
  - CLAIM-16
  - CLAIM-17
  evidence:
  - type: ledger
    ref: sg:854:8892fdc4cc4bee3f
  - type: ledger
    ref: sg:861:7004f70221a62847
  - type: ledger
    ref: sg:862:1c8867ccb0cdaa2a
- control: DSP-05
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'The ledger records data flow, not only actions: a read, a transform and a write are
    linked by an explicit derivation edge naming the artifacts and the script that joined them. SURFACE
    NARROWED 2026-08-05 (R2-003). This claim previously declared hook:PostToolUse; its proof exercises
    the mechanism directly rather than driving that hook, and registration is not execution. The surface
    is withdrawn rather than asserted. Deployed-hook behaviour is a separate claim, and it has not been
    made; No statically detectable network call site exists in the shipped Python package except the named
    opt-in anchoring exception. This is a source audit, not a proof of no network activity. It cannot
    see indirect subprocess calls, shell network programs, dynamic imports or commands assembled at runtime.
    SUB-CLAIM WITHDRAWN 2026-08-04 (SG-HARD-039): this also asserted "and CI performs no fetch", which
    is false. CI runs actions/checkout, actions/setup-python and pip install, every one of which resolves
    a remote action or package. The proof equated the absence of literal curl and wget text with the absence
    of fetching. Build-time dependency retrieval is real and is not claimed away. Proven by 2 record(s)
    in this toolchain''s own keyed chain-of-custody ledger (sg:846:90609585c37284cb, sg:863:c55bb0a4b78ade09),
    each produced by an executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-01
  - CLAIM-18
  evidence:
  - type: ledger
    ref: sg:846:90609585c37284cb
  - type: ledger
    ref: sg:863:c55bb0a4b78ade09
- control: DSP-20
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'The ledger records data flow, not only actions: a read, a transform and a write are
    linked by an explicit derivation edge naming the artifacts and the script that joined them. SURFACE
    NARROWED 2026-08-05 (R2-003). This claim previously declared hook:PostToolUse; its proof exercises
    the mechanism directly rather than driving that hook, and registration is not execution. The surface
    is withdrawn rather than asserted. Deployed-hook behaviour is a separate claim, and it has not been
    made; Accumulated session taint denies an egress that the identical call would have been allowed to
    make earlier in the same session, the denial names the contributing artifacts, and this holds through
    the real hook across separate processes and with the state cache deleted; `steer` asks on first touch
    of a classified artifact and offers a recorded script delegation; it does not deny the first read.
    SURFACE NARROWED 2026-08-05 (R2-003). This claim previously declared hook:PreToolUse; its proof exercises
    the mechanism directly rather than driving that hook, and registration is not execution. The surface
    is withdrawn rather than asserted. Deployed-hook behaviour is a separate claim, and it has not been
    made; The delegation sequence enforces test-before-live-data, and refuses a script whose digest changed
    after its test passed. NARROWED 2026-08-04 (SG-HARD-018/019). This holds against a cooperative script,
    not an adversarial one. The security subject is a mutable pathname, so a concurrent replacer can swap
    the bytes between the digest check and execution; and the paired test itself runs unsigned and can
    rewrite the script it has just certified. Immutable content-addressed execution is required before
    this is a security property rather than a discipline; Under `bar` the model receives only a handle
    or summary, and an unsigned script is refused. SURFACE NARROWED 2026-08-05 (R2-003). This claim previously
    declared hook:PreToolUse; its proof exercises the mechanism directly rather than driving that hook,
    and registration is not execution. The surface is withdrawn rather than asserted. Deployed-hook behaviour
    is a separate claim, and it has not been made; The blank AI-CAIQ template is never modified. A fill
    leaves its digest and mtime unchanged, and an altered template refuses regeneration. Proven by 6 record(s)
    in this toolchain''s own keyed chain-of-custody ledger (sg:846:90609585c37284cb, sg:852:a9fb3c60b2fa3a61,
    sg:853:e97f062f71d01a32, sg:854:8892fdc4cc4bee3f, sg:855:71a7902ef91a590d, sg:860:1309b4778cefca07),
    each produced by an executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-01
  - CLAIM-07
  - CLAIM-08
  - CLAIM-09
  - CLAIM-10
  - CLAIM-15
  evidence:
  - type: ledger
    ref: sg:846:90609585c37284cb
  - type: ledger
    ref: sg:852:a9fb3c60b2fa3a61
  - type: ledger
    ref: sg:853:e97f062f71d01a32
  - type: ledger
    ref: sg:854:8892fdc4cc4bee3f
  - type: ledger
    ref: sg:855:71a7902ef91a590d
  - type: ledger
    ref: sg:860:1309b4778cefca07
- control: I&S-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'No statically detectable network call site exists in the shipped Python package except
    the named opt-in anchoring exception. This is a source audit, not a proof of no network activity.
    It cannot see indirect subprocess calls, shell network programs, dynamic imports or commands assembled
    at runtime. SUB-CLAIM WITHDRAWN 2026-08-04 (SG-HARD-039): this also asserted "and CI performs no fetch",
    which is false. CI runs actions/checkout, actions/setup-python and pip install, every one of which
    resolves a remote action or package. The proof equated the absence of literal curl and wget text with
    the absence of fetching. Build-time dependency retrieval is real and is not claimed away. Proven by
    1 record(s) in this toolchain''s own keyed chain-of-custody ledger (sg:863:c55bb0a4b78ade09), each
    produced by an executable procedure and re-verified by `stop-guessing claims check`.'
  claims:
  - CLAIM-18
  evidence:
  - type: ledger
    ref: sg:863:c55bb0a4b78ade09
- control: LOG-03
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: 'A keyed chain defeats truncate-and-recompute. Without the session key held in the OS
    keychain, a forged chain that is internally consistent still fails verification; A record missing
    the `alterations` key is refused at write, while `alterations: []` is accepted — an empty list asserts
    nothing was altered, a missing key means nobody looked; `verify --sufficiency` reports `incomplete`
    and names the unpopulated evidence regime rather than claiming sufficiency it cannot back; Execution
    claims made by the agent are reconciled against the hook-side dispatch ledger; a fabricated or replayed
    claim is detected and escalated to a human. SURFACE NARROWED 2026-08-05 (R2-003). This claim previously
    declared hook:Stop; its proof exercises the mechanism directly rather than driving that hook, and
    registration is not execution. The surface is withdrawn rather than asserted. Deployed-hook behaviour
    is a separate claim, and it has not been made. Proven by 4 record(s) in this toolchain''s own keyed
    chain-of-custody ledger (sg:847:41824c299ad00074, sg:850:3dc422f62c5bd481, sg:851:22f751f9dc2fae59,
    sg:858:3831940b083ee1b4), each produced by an executable procedure and re-verified by `stop-guessing
    claims check`.'
  claims:
  - CLAIM-02
  - CLAIM-05
  - CLAIM-06
  - CLAIM-13
  evidence:
  - type: ledger
    ref: sg:847:41824c299ad00074
  - type: ledger
    ref: sg:850:3dc422f62c5bd481
  - type: ledger
    ref: sg:851:22f751f9dc2fae59
  - type: ledger
    ref: sg:858:3831940b083ee1b4
- control: LOG-10
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: A keyed chain defeats truncate-and-recompute. Without the session key held in the OS
    keychain, a forged chain that is internally consistent still fails verification; The sink refuses
    to append onto a broken or truncated chain, rather than burying the break under new entries; Sealing
    archives a segment and chains the next segment to the sealed digest; no ledger rotation ever truncates
    history; PATH-shadowing the CLI, direct ledger writes, hook-script substitution and daemon kill are
    each refused or detected. NARROWED 2026-08-04 (SG-HARD-006/007/010). This is no longer the broader
    claim that the recorder is unreachable by the agent it records. At the tiers the installer can actually
    deliver (0 and 1) the recorder runs under the agent's own uid, the chain key is a mode-600 file that
    anything the agent runs can read, and the socket authenticates ordering rather than the caller. The
    four named attacks are refused or detected. An agent that simply reads the key is not among them;
    The blank AI-CAIQ template is never modified. A fill leaves its digest and mtime unchanged, and an
    altered template refuses regeneration; A full uninstall removes hooks and registrations while preserving
    the accumulated ledger and observation log. Proven by 6 record(s) in this toolchain's own keyed chain-of-custody
    ledger (sg:847:41824c299ad00074, sg:848:b5df7a32140d368c, sg:849:47e3eb06a6e56040, sg:857:ddf9c21d14dc3079,
    sg:860:1309b4778cefca07, sg:864:18e115ac5361c74c), each produced by an executable procedure and re-verified
    by `stop-guessing claims check`.
  claims:
  - CLAIM-02
  - CLAIM-03
  - CLAIM-04
  - CLAIM-12
  - CLAIM-15
  - CLAIM-19
  evidence:
  - type: ledger
    ref: sg:847:41824c299ad00074
  - type: ledger
    ref: sg:848:b5df7a32140d368c
  - type: ledger
    ref: sg:849:47e3eb06a6e56040
  - type: ledger
    ref: sg:857:ddf9c21d14dc3079
  - type: ledger
    ref: sg:860:1309b4778cefca07
  - type: ledger
    ref: sg:864:18e115ac5361c74c
- control: LOG-12
  answer: 'No'
  ssrm: Owned by OSP
  implementation: 'No. There is artifact-level and decision-level provenance, but no continuous activity
    log for cryptographic key lifecycle events specifically. Evidence searched: stop_guessing/attest/keys.py,
    stop_guessing/ledger/, rules/, policy/coc.policy.d/.'
  claims:
  - CLAIM-04
  evidence:
  - type: ledger
    ref: sg:849:47e3eb06a6e56040
- control: MDS-13
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: Under `bar` the model receives only a handle or summary, and an unsigned script is refused.
    SURFACE NARROWED 2026-08-05 (R2-003). This claim previously declared hook:PreToolUse; its proof exercises
    the mechanism directly rather than driving that hook, and registration is not execution. The surface
    is withdrawn rather than asserted. Deployed-hook behaviour is a separate claim, and it has not been
    made. Proven by 1 record(s) in this toolchain's own keyed chain-of-custody ledger (sg:855:71a7902ef91a590d),
    each produced by an executable procedure and re-verified by `stop-guessing claims check`.
  claims:
  - CLAIM-10
  evidence:
  - type: ledger
    ref: sg:855:71a7902ef91a590d
- control: SEF-01
  answer: 'Yes'
  ssrm: Owned by OSP
  implementation: Execution claims made by the agent are reconciled against the hook-side dispatch ledger;
    a fabricated or replayed claim is detected and escalated to a human. SURFACE NARROWED 2026-08-05 (R2-003).
    This claim previously declared hook:Stop; its proof exercises the mechanism directly rather than driving
    that hook, and registration is not execution. The surface is withdrawn rather than asserted. Deployed-hook
    behaviour is a separate claim, and it has not been made. Proven by 1 record(s) in this toolchain's
    own keyed chain-of-custody ledger (sg:858:3831940b083ee1b4), each produced by an executable procedure
    and re-verified by `stop-guessing claims check`.
  claims:
  - CLAIM-13
  evidence:
  - type: ledger
    ref: sg:858:3831940b083ee1b4
- control: STA-09
  answer: 'No'
  ssrm: Owned by OSP
  implementation: 'No. A service Bill of Materials is not emitted. The in-toto Statement envelope and
    the pinned MANIFEST.sha256 over the vendored tree are adjacent but are not an SBOM. Evidence searched:
    stop_guessing/attest/, stop_guessing/compat/, .github/workflows/.'
  claims:
  - CLAIM-14
  - CLAIM-20
  evidence:
  - type: ledger
    ref: sg:859:02e44cb93e7a4560
  - type: ledger
    ref: sg:865:17fce297e11b3fd7
proposed_agentic_controls:
  note: 'CSA''s DRAFT agentic controls (labs.cloudsecurityalliance.org). Proposed, not published: they
    do not exist in the AICM v1.1.0 workbook and are NOT written into it. Recorded here because the evidence
    is real and the gap analysis is CSA''s own, but presenting them as AICM controls would be a fabrication.'
  answers:
  - control: IAM-AG-03
    answer: 'Yes'
    ssrm: Owned by OSP
    implementation: PATH-shadowing the CLI, direct ledger writes, hook-script substitution and daemon
      kill are each refused or detected. NARROWED 2026-08-04 (SG-HARD-006/007/010). This is no longer
      the broader claim that the recorder is unreachable by the agent it records. At the tiers the installer
      can actually deliver (0 and 1) the recorder runs under the agent's own uid, the chain key is a mode-600
      file that anything the agent runs can read, and the socket authenticates ordering rather than the
      caller. The four named attacks are refused or detected. An agent that simply reads the key is not
      among them. Proven by 1 record(s) in this toolchain's own keyed chain-of-custody ledger (sg:857:ddf9c21d14dc3079),
      each produced by an executable procedure and re-verified by `stop-guessing claims check`.
    claims:
    - CLAIM-12
    evidence:
    - type: ledger
      ref: sg:857:ddf9c21d14dc3079
  - control: LOG-AG-01
    answer: 'Yes'
    ssrm: Owned by OSP
    implementation: 'The ledger records data flow, not only actions: a read, a transform and a write are
      linked by an explicit derivation edge naming the artifacts and the script that joined them. SURFACE
      NARROWED 2026-08-05 (R2-003). This claim previously declared hook:PostToolUse; its proof exercises
      the mechanism directly rather than driving that hook, and registration is not execution. The surface
      is withdrawn rather than asserted. Deployed-hook behaviour is a separate claim, and it has not been
      made; Session custody state rebuilt from the ledger alone reproduces the pre-compaction digest exactly;
      state never depends on the transcript. SURFACE NARROWED 2026-08-05 (R2-003). This claim previously
      declared hook:PreCompact; its proof exercises the mechanism directly rather than driving that hook,
      and registration is not execution. The surface is withdrawn rather than asserted. Deployed-hook
      behaviour is a separate claim, and it has not been made. Proven by 2 record(s) in this toolchain''s
      own keyed chain-of-custody ledger (sg:846:90609585c37284cb, sg:856:41f140e203df2d89), each produced
      by an executable procedure and re-verified by `stop-guessing claims check`.'
    claims:
    - CLAIM-01
    - CLAIM-11
    evidence:
    - type: ledger
      ref: sg:846:90609585c37284cb
    - type: ledger
      ref: sg:856:41f140e203df2d89
  - control: LOG-AG-02
    answer: 'Yes'
    ssrm: Owned by OSP
    implementation: Accumulated session taint denies an egress that the identical call would have been
      allowed to make earlier in the same session, the denial names the contributing artifacts, and this
      holds through the real hook across separate processes and with the state cache deleted. Proven by
      1 record(s) in this toolchain's own keyed chain-of-custody ledger (sg:852:a9fb3c60b2fa3a61), each
      produced by an executable procedure and re-verified by `stop-guessing claims check`.
    claims:
    - CLAIM-07
    evidence:
    - type: ledger
      ref: sg:852:a9fb3c60b2fa3a61
