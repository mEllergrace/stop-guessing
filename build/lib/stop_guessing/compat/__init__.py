"""Byte-identical vendored no-noodles, plus the gate that proves supersession is lossless.

`nonoodles/` is a verbatim copy of moonsoup/no-noodles. It is never edited here. Upstream fixes
land upstream and are re-vendored; local edits would silently fork the behaviour this package
exists to preserve.
"""
